{
  "name": "@hexlet/code",
  "version": "1.0.0",
  "description": "Slack project",
  "main": "index.js",
  "proxy": "http://localhost:5001",
  "scripts": {
    "build": "npm run build --prefix frontend",
    "start": "npx start-server -s ./frontend/build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "cd frontend && npm ci"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/eldaromv/frontend-project-12.git"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@hexlet/chat-server": "^2.0.4"
  }
}